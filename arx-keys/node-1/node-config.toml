# ARX Node 1 configuration for local development
# Connects to Surfpool RPC running on the host machine

[node]
offset = 0
hardware_claim = 0
starting_epoch = 0
ending_epoch = 9223372036854775807

[network]
address = "0.0.0.0"

[solana]
# Points to Surfpool running on the host via Docker's host gateway
endpoint_rpc = "http://host.docker.internal:8899"
endpoint_wss = "ws://host.docker.internal:8900"
cluster = "Localnet"
commitment.commitment = "confirmed"
